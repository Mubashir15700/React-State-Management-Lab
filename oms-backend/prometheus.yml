global:
  scrape_interval: 10s # How often to "ping" your app for data

scrape_configs:
  - job_name: 'nestjs-oms-backend'
    static_configs:
      - targets: ['host.docker.internal:3000'] # 'host.docker.internal' allows Docker to see your local NestJS app
